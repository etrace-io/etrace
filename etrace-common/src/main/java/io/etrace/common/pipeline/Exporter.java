package io.etrace.common.pipeline;

/**
 * Pipeline Exporter: possible exporter implement:
 * <p>
 * Queue based exporter (like kafka/rabbitmq) DB (like mysql) Hadoop (like HDFS, HBase)
 */
public interface Exporter {
}
